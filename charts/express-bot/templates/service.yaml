apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-bot.fullname" . }}
  labels:
    {{- include "express-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "express-bot.selectorLabels" . | nindent 4 }}
